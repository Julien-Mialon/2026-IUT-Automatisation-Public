# Base URL of the API. Leave empty to use the Vite dev-server proxy (see vite.config.ts).
VITE_API_BASE_URL=

# Where the dev-server proxy forwards /api during `yarn dev`.
VITE_DEV_API_PROXY=http://localhost:5000
